<!-- Default Page -->
<section class="default-page">
  <h1>{{ page.title }}</h1>
  <div>{{ page.content }}</div>
</section>

{% schema %}
{
  "name": "Default Page",
  "settings": [],
  "presets": [{ "name": "Default Page" }]
}
{% endschema %}
